from django.apps import AppConfig


class DifficultyConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.difficulty"
    verbose_name = "Difficulty Prediction"
